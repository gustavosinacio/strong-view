import { SxProps } from "@mui/material";
import { CSSProperties } from "styled-components";

export const ChangePageButtonSX: SxProps = {
  marginTop: "10px",
  fontSize: "12px",
};

export const CenterContent: CSSProperties = {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};
